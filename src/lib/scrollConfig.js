export const TAGLINES = [
  { variant: 'headline' },                    // 0% — just the H1 question
  { text: 'Because I’m always…', emphasis: true }, // ~30% — the setup
  { text: 'One keystroke ahead.', emphasis: true }, // ~60% — the punchline
]

export const SCROLL = {
  taglineStart: 0.05,
  taglineEnd: .6,
  scaleStart: 0.75,
  scaleEnd: 0.95,
  scaleFrom: 1,
  scaleTo: 0.85,
  radiusTo: 24,
  scrollCueFadeEnd: 0.3,
  sectionHeight: '1000vh',
}
